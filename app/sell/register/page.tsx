'use client'

import { useEffect, useState } from 'react'
import { useFormState, useFormStatus } from 'react-dom'
import Link from 'next/link'
import { registerSeller, type ActionState } from '@/app/actions/auth'

function SubmitButton() { const { pending } = useFormStatus(); return <button type="submit" disabled={pending} className="btn-primary w-full disabled:opacity-50">{pending ? 'Submitting application…' : 'Submit seller application →'}</button> }
function Upload({ name, label, accept = 'image/jpeg,image/png,application/pdf' }: { name: string; label: string; accept?: string }) { return <label className="block"><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">{label} *</span><input name={name} type="file" accept={accept} required className="block w-full rounded-xl border border-dashed border-slate-300 bg-slate-50 px-3 py-3 text-sm" /></label> }

export default function SellerRegisterPage() {
  const [state, formAction] = useFormState<ActionState, FormData>(registerSeller, {})
  const [areas, setAreas] = useState<any[]>([])
  useEffect(() => { fetch('/api/store-areas', { cache: 'no-store' }).then(r => r.json()).then(d => setAreas(d.areas || [])).catch(() => setAreas([])) }, [])
  return <main className="min-h-[calc(100vh-160px)] bg-[#f7f8f6] py-8 sm:py-12"><div className="page-wrap grid max-w-6xl gap-8 lg:grid-cols-[.7fr_1.3fr] lg:items-start"><aside className="rounded-3xl bg-[#14251e] p-7 text-white sm:p-9 lg:sticky lg:top-28"><span className="eyebrow bg-white/10 text-[#d9f33f]">Sell on jojokev</span><h1 className="mt-6 text-4xl font-black leading-none tracking-[-.06em] sm:text-5xl">Partner with<br /><span className="text-[#d9f33f]">Jojokev.</span></h1><p className="mt-5 text-sm leading-6 text-white/65">Submit your identity and store details. Our team reviews your application before you can list products.</p><Link href="/" className="mt-10 inline-block text-sm font-bold text-[#d9f33f] hover:underline">← Back to shopping</Link></aside><section className="surface p-6 sm:p-9"><p className="section-kicker">Seller verification</p><h2 className="text-3xl font-black tracking-tight">Create your seller account</h2><p className="mt-2 text-sm text-slate-500">Your documents are private and reviewed only by authorized Jojokev staff.</p>{state.error && <div className="mt-5 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm font-semibold text-red-700">{state.error}</div>}{state.success && <div className="mt-5 rounded-xl border border-emerald-200 bg-[#e5f6ee] px-4 py-3 text-sm font-semibold text-[#0b5d43]">{state.success} <Link href="/login" className="underline">Log in →</Link></div>}<form action={formAction} encType="multipart/form-data" className="mt-6 space-y-7"><section><h3 className="text-sm font-black">Personal information</h3><div className="mt-3 grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">First name *</span><input name="first_name" required className="field" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Last name *</span><input name="last_name" required className="field" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Email *</span><input name="email" type="email" required className="field" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Phone number *</span><input name="phone" type="tel" required className="field" placeholder="0800 000 0000" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Password *</span><input name="password" type="password" required minLength={8} className="field" /></label></div></section><section className="border-t pt-6" style={{ borderColor: 'var(--line)' }}><h3 className="text-sm font-black">Identity and address</h3><div className="mt-3 grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Date of birth *</span><input name="date_of_birth" type="date" required className="field" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Gender *</span><select name="gender" required className="field"><option value="">Choose gender</option><option>Female</option><option>Male</option><option>Prefer not to say</option></select></label><label className="sm:col-span-2"><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Residential address *</span><textarea name="residential_address" required className="field min-h-24" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Identification type *</span><select name="id_type" required className="field"><option value="">Choose ID</option><option>NIN</option><option>Voters card</option><option>Drivers license</option></select></label><Upload name="id_photo" label="Photo of identification" /><Upload name="utility_bill" label="Proof of address / utility bill" /><Upload name="passport_photo" label="Passport photo" /><Upload name="signature_photo" label="Photo of signature" /></div></section><section className="border-t pt-6" style={{ borderColor: 'var(--line)' }}><h3 className="text-sm font-black">Store information</h3><div className="mt-3 grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Business / store name *</span><input name="business_name" required className="field" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Store area *</span><select name="store_area_id" required className="field"><option value="">Choose an approved store area</option>{areas.map(a => <option key={a.id} value={a.id}>{a.name} — {a.city}, {a.state}</option>)}</select>{!areas.length && <span className="mt-1 block text-xs text-amber-700">No store areas yet. Admin must add a Store Area first.</span>}</label><label className="sm:col-span-2"><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Store address *</span><textarea name="business_address" required className="field min-h-24" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Store phone *</span><input name="business_phone" type="tel" required className="field" /></label></div></section><section className="border-t pt-6" style={{ borderColor: 'var(--line)' }}><h3 className="text-sm font-black">Payout details</h3><div className="mt-3 grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Bank name</span><input name="bank_name" className="field" /></label><label><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Account number</span><input name="account_number" inputMode="numeric" className="field" /></label><label className="sm:col-span-2"><span className="mb-1.5 block text-xs font-extrabold uppercase tracking-wide text-slate-600">Account name</span><input name="account_name" className="field" /></label></div></section><SubmitButton /></form><p className="mt-6 text-center text-sm text-slate-500">Already have an account? <Link href="/login" className="font-bold text-[#14805d] hover:underline">Log in</Link></p></section></div></main>
}
